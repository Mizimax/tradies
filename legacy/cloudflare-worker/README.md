# Legacy Cloudflare Worker Implementation

This folder preserves the earlier TypeScript Cloudflare/OANDA/MetaApi implementation for reference.

It is no longer the primary production architecture. The active path is now:

```text
MQL5 Expert Advisor -> MT5 Terminal -> Broker
```

Do not deploy this Worker path unless intentionally reviving the legacy architecture.
